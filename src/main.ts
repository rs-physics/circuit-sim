// Entry point for the application.
// Responsible only for bootstrapping the App.

import "./style.css";
import { App } from "./app/app";

/**
 * Application bootstrap.
 *
 * Keep this file minimal.
 * All real logic lives inside App.
 */
new App().start();
